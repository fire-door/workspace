# 2026-03-07 记忆

## 神卓互联内网穿透配置

### 完成的工作
- 在树莓派上安装并配置了神卓互联客户端
- 安装路径：`/usr/local/shenzhuo/`
- 客户端版本：V9.3.8.1 (Linux arm64)
- 配置了 systemd 服务，已设置开机自启
- 服务状态：运行中 (active)
- 服务名称：shenzhuo.service

### 配置详情
- 账号：17682361171
- 服务文件：`/etc/systemd/system/shenzhuo.service`
- 日志目录：`/usr/local/shenzhuo/log/`

### 常用命令
```bash
# 查看服务状态
sudo systemctl status shenzhuo

# 重启服务
sudo systemctl restart shenzhuo

# 停止服务
sudo systemctl stop shenzhuo

# 查看日志
sudo journalctl -u shenzhuo -n 50
```

### 注意事项
- 服务以 root 权限运行
- 客户端会在 `/usr/local/shenzhuo/log/` 目录生成日志文件
- 如需修改账号密码，编辑 `/etc/systemd/system/shenzhuo.service` 文件中的 `ExecStart` 行，然后执行 `systemctl daemon-reload && systemctl restart shenzhuo`
